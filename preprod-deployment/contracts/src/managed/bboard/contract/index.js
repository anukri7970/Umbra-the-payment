// STUB — overwritten by `compact compile` during Vercel / CI build.
// Committed so that `npm run typecheck`, `npm run test`, and `npm run build`
// all pass in environments that don't have the Compact toolchain installed.

export class Contract {
  // eslint-disable-next-line no-unused-vars
  constructor(_witnesses) {}
}

export const contractConfig = {};
export const witnesses = {};
